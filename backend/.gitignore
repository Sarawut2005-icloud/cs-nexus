
# dev
.env
dist
src/generated

# dev storage
storage-dev/

# openapi.json ต้อง **ขึ้น repo** — อย่าใส่กลับมาที่นี่
#
# มาตรฐาน CSMJU2030 หน้า 13 บังคับให้ contract เผยแพร่พร้อมโค้ด ระบบย่อยอื่น
# อ่านไฟล์นี้เพื่อเขียนโค้ดฝั่งตัวเอง และ CI job "openapi ตรงกับโค้ด" ก็เทียบ
# ไฟล์ในรีโปกับสิ่งที่โค้ดสร้างจริง — ถ้า ignore ไว้ ตัวตรวจนั้นก็ไม่มีอะไรให้เทียบ
